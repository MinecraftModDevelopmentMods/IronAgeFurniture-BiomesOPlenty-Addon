# Gradle and Buildship must select a local Adoptium Java 25 daemon before
# evaluating build.gradle. Exact 25.0.3+9 compiler metadata is verified by the
# build after the daemon starts; no daemon JVM is downloaded from this file.
toolchainVendor=ADOPTIUM
toolchainVersion=25
