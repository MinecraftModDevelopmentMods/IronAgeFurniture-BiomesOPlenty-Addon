name: IAF Biomes O Plenty Add-on 26.3 Forge CI

on:
  push:
    branches:
      - master-26.3
      - 'feature/**'
  pull_request:
    branches:
      - master-26.3

permissions:
  contents: read

concurrency:
  group: iaf-bop-addon-26.3-forge-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cold-bootstrap:
    name: Cold and offline-repeat bootstrap
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install exact Java 25
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: '25.0.3+9.0.LTS'
      - name: Resolve once from an empty cache and repeat offline
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          export GRADLE_USER_HOME="$RUNNER_TEMP/iaf-bop-addon-cold-cache"
          rm -rf "$GRADLE_USER_HOME"
          args=(
            --no-daemon --no-build-cache --stacktrace --max-workers=2
            "-Dorg.gradle.java.installations.paths=$JAVA_HOME"
            -Dorg.gradle.java.installations.auto-detect=false
            -Dorg.gradle.java.installations.auto-download=false
          )
          ./gradlew compileJava verifyPinnedCoreArtifact verifyMavenizerCompatibilityFixture "${args[@]}"
          ./gradlew compileJava verifyPinnedCoreArtifact verifyMavenizerCompatibilityFixture --offline "${args[@]}"

  build:
    name: Build, test, and audit
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: Install exact Java 25
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
        with:
          distribution: temurin
          java-version: '25.0.3+9.0.LTS'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6
      - name: Build, test, document, and audit
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          args=(
            --no-daemon --stacktrace --max-workers=2
            "-Dorg.gradle.java.installations.paths=$JAVA_HOME"
            -Dorg.gradle.java.installations.auto-detect=false
            -Dorg.gradle.java.installations.auto-download=false
          )
          ./gradlew clean check build javadoc verifyFurnitureCatalog verifyReleaseArtifacts \
            writeReleaseChecksums verifyReleaseChecksums prepareEclipse \
            verifyEclipseProductionClasspath "${args[@]}"
      - name: Run legacy migration GameTests
        shell: bash
        run: |
          set -euo pipefail
          args=(
            --no-daemon --stacktrace --max-workers=2
            "-Dorg.gradle.java.installations.paths=$JAVA_HOME"
            -Dorg.gradle.java.installations.auto-detect=false
            -Dorg.gradle.java.installations.auto-download=false
          )
          ./gradlew runGameTestServer -PgameTestRunDirectory=build/game-test-run "${args[@]}"
      - name: Upload audited release candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: iaf-bop-addon-26.3-forge-${{ github.sha }}
          if-no-files-found: error
          retention-days: 30
          path: |
            build/libs/iron-age-furniture-biomes-o-plenty-addon-0.3.0.2603001.jar
            build/libs/iron-age-furniture-biomes-o-plenty-addon-0.3.0.2603001-sources.jar
            build/libs/iron-age-furniture-biomes-o-plenty-addon-0.3.0.2603001-javadoc.jar
            build/release/SHA256SUMS
            CHANGELOG.md
      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: iaf-bop-addon-diagnostics-${{ github.sha }}
          if-no-files-found: ignore
          retention-days: 14
          path: |
            build/test-results/**
            build/reports/**
            run/logs/**
            build/game-test-run/logs/**
            build/game-test-run/crash-reports/**
